[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
loyalty_engine = { path = "../loyalty_engine" }
axum = "0.7"
tracing = "0.1"

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"